import 'package:flutter/material.dart';

class ScanCertScreen extends StatelessWidget {
  const ScanCertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Scan Certificate Screen')),
    );
  }
}
