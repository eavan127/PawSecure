import 'package:flutter/material.dart';

class StrayIdentityScreen extends StatelessWidget {
  const StrayIdentityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Stray Identity Screen')),
    );
  }
}
